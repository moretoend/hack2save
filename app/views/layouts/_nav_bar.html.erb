<!--  NAVBAR BEGIN -->
<div class="navbar-fixed">
  <nav>
    <div class="nav-wrapper">

      <div class="row">

        <div class="col s12 m12 l4">
          <ul class="left">
            <li>
              <a href="" data-activates="slide-out" class="menu-collapse">
                <i class="material-icons">menu</i>
              </a>
            </li>
            <li>
              <a href="#!" class="brand-logo">
                <i class="material-icons left">favorite</i>
                Hack2Save
              </a>
            </li>
          </ul>
        </div>

        <div class="input-field col s12 m12 l4 hide-on-med-and-down">
          <i class="material-icons prefix">search</i>
          <input id="icon_prefix" type="text" class="validate">
          <label for="icon_prefix">Search...</label>
        </div>

        <div class="input-field col s12 m12 l4 hide-on-med-and-down" >
          <%= form_tag set_institution_institutions_path, method: :post, id: "select_institution" do |f| %>
            <%= select_tag :institution_id, 
                           options_for_select(Institution.all.map { |item| [item.name, item.id] }, session[:institution]),
                           onchange: "$('#select_institution').submit()" %>
          <% end %>
          <!-- <label>Filter by institutions</label> -->
        </div>
      </div>
    </div>
  </nav>
</div>
<!-- NAVBAR END -->
